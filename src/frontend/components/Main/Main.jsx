import React from 'react';
import styled from 'styled-components';

const Main = () => (
  <Container>
    로그인 되었을때 ! ! !
  </Container>
);

const Container = styled.div`
    width: 200px;
    height: 500px;
    background: black;
    color: white;
`;

export default Main;
